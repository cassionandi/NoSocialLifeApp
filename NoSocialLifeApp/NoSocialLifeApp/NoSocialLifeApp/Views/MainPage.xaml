<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TelasMaratonaIntermediariaApp"
             x:Class="NoSocialLifeApp.Views.MainPage">

    <StackLayout VerticalOptions="CenterAndExpand"
                 HorizontalOptions="Fill">
        <Button 
        Text="Ver 50 Mais Populares"
            Command="{Binding ShowPopularesCommand}"
        />

        <Button 
        Text="Buscar"
            Command="{Binding ShowBuscaCommand}"
        />
    </StackLayout>

</ContentPage>